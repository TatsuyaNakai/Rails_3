<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%# @users.each do |user| %>
  <%#= render user %>
  <!--eachとuserの繰り返し処理を@usersだけで書き換えることができる。 Railsが自動保管してくれる。-->
  <%= render @users %>
  <%# end %>
</ul>

<%= will_paginate %>
